import BlogPostClient from './BlogPostClient';

export const metadata = {
  title: 'Blog | High Noon Optimist Club',
  description:
    'Read news, stories, and updates from the High Noon Optimist Club in Regina, including youth programs and local community events.',
};

export default function BlogPostPage() {
  return <BlogPostClient />;
}
